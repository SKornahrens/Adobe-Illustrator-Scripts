    var docRef = app.activeDocument;    var selectRef = docRef.pathItems;    var groupThis = app.selection;//paste current clipboard content app.paste();//Group pasted content   var makeGroup = app.activeDocument.groupItems.add();    var groupThis = app.activeDocument.selection;      for (i = 0; i < groupThis.length; i++){      groupThis[i].move (makeGroup, ElementPlacement.PLACEATBEGINNING);      };//Rotate Groupvar alterGroup = docRef.groupItems[0];alterGroup.selected = true;alterGroup.rotate(-90);//fits art board to groups widthvar inch = 72;var setWidth = docRef.selection[0].width;var setHeight =docRef.selection[0].height; docRef.artboards[0].artboardRect = [ 0, 877.68, setWidth, 0];//center group app.coordinateSystem = CoordinateSystem.ARTBOARDCOORDINATESYSTEM;  var activeAB = docRef.artboards.getActiveArtboardIndex();  var centerAB = docRef.artboards[activeAB].artboardRect;  var groupmove = docRef.selection[0];  groupmove.position = new Array ((centerAB[2]-centerAB[0])/2 - groupmove.width/2, (centerAB[3]-centerAB[1])/2  + groupmove.height/2);//ungroup